extend type Mutation {
    createRole(
        name: String,
        ability_names: [String],
        group: Int
    ): Role!
    
    updateRole(
        id: ID,
        name: String,
        ability_names: [String],
        group: Int
    ): Role!
}