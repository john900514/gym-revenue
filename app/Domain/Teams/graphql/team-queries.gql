extend type Query {
    teams(
        filter: Filter @scope
        orderBy: [OrderByClause!] @orderBy
    ): [Team!]!
    @paginate(defaultCount: 10)
        @can(ability: "teams.read", model: "App\\Domain\\Teams\\Models\\Team")

    team (
        id: ID @eq
    ): Team @find
}