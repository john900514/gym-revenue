#import lead.graphql

"Lead Sources"
type LeadSource {
    "Unique primary key."
    id: ID!

    "Non-unique name."
    name: String!

    "When the account was created."
    created_at: DateTime!

    "When the account was last updated."
    updated_at: DateTime!
}