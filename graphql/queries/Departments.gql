extend type Query {
    departments(
        "Filters by name. Accepts SQL LIKE wildcards `%` and `_`."
        filter: Filter @scope
    ): [Department!]!
    @orderBy(column: "name")
    @paginate(defaultCount: 10)
        @can(ability: "departments.read", model: "App\\Domain\\Departments\\Department")
}