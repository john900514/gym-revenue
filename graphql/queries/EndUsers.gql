extend type Query {
    endUsers(
        filter: Filter @scope
        orderBy: [OrderByClause!] @orderBy
    ): [EndUser!]!
    @paginate(defaultCount: 10)
        @can(ability: "leads.read", model: "App\\Domain\\EndUsers\\Projections\\EndUser")
}
