extend type Query {
    leads(
        filter: Filter @scope
    ): [Lead!]!
    @paginate(defaultCount: 10)
        @can(ability: "leads.read", model: "App\\Domain\\EndUsers\\Leads\\Projections\\Lead")
}