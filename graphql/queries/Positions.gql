extend type Query {
    positions(
        "Filters by name. Accepts SQL LIKE wildcards `%` and `_`."
        filter: Filter @scope
    ): [Position!]!
    @orderBy(column: "name")
    @paginate(defaultCount: 10)
        @can(ability: "positions.read", model: "App\\Models\\Position")
}